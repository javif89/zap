{
  "name": "zap-theme",
  "version": "1.0.0",
  "description": "The default theme for zap",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "concurrently \"cargo watch -x 'run -p theme'\" \"npx tailwindcss -i ./src/input.css -o ./out/style.css --watch\" \"npx live-server out\""
  },
  "author": "Javier Feliz",
  "license": "ISC",
  "devDependencies": {
    "@tailwindcss/typography": "^0.5.16"
  }
}